[pytest]
; The canonical suite: the scorecard (numbers) + the probes
; (mechanisms).  Substrate-dynamics probes evolve the NLS equation
; for minutes and run on demand:  pytest -q -m slow
testpaths = tests
addopts = -m "not slow"
markers =
    slow: substrate-dynamics probes (minutes of PDE evolution)
